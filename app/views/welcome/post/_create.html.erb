said it was 
<strong>
<%= activity.trackable.content %>
</strong>
at <%=link_to Building.find(Room.find(activity.trackable.room_id.to_i).building_id.to_i).name ,  Building.find(Room.find(activity.trackable.room_id.to_i).building_id.to_i) %>

on <%= activity.trackable.created_at.strftime("%e %b %Y %H:%m:%S%p") %>